/**
 * Vector represented as a readonly array of numbers
 */
export type Vector = readonly number[];

/**
 * Matrix represented as a readonly 2D array of numbers (row-major order)
 */
export type Matrix = readonly (readonly number[])[];
